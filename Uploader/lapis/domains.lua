domains = {}
domains.ndomains = {""}
domains.wildcard = {""}
-- Add domains like this: domains = {"domain1.tld", "domain2.tld" ...}
-- If you have a wildcard domain remember to add it to both tables!
return domains
